// Registers @testing-library/jest-dom matchers (toBeInTheDocument, toHaveValue...)
// with Vitest. Used by the Angular unit-test builder (see setupFiles in angular.json).
import '@testing-library/jest-dom/vitest';
